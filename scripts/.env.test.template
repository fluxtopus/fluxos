# Optional environment variables for local test/E2E runs.
#
# Copy to a local file (gitignored) and source it:
#   cp scripts/.env.test.template .env.test
#   set -a && source .env.test && set +a
#
# Then run:
#   ./scripts/run-all-tests.sh --unit
#   (cd packages/aios-stripe && pytest tests/e2e -m e2e -v)

# ============================================================================
# Stripe (Test Mode)
# ============================================================================
# Get from: https://dashboard.stripe.com/test/apikeys
# MUST start with "sk_test_" for safety
STRIPE_SECRET_KEY=sk_test_your_key_here

# Webhook secret for local testing (from Stripe CLI)
# Run: stripe listen --forward-to localhost:8006/api/v1/gateway/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Test price ID for checkout tests
STRIPE_TEST_PRICE_ID=price_test_your_price_id

# ============================================================================
# Test Configuration
# ============================================================================
TEST_EMAIL=your-email@example.com

# URLs (usually defaults are fine for local testing)
LANDING_URL=http://localhost:3002
TENTACKL_URL=http://localhost:8005
INKPASS_URL=http://localhost:8004
MIMIC_URL=http://localhost:8006
