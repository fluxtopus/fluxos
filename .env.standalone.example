# Standalone Tentackl Configuration
# Copy to .env.standalone and customize.
#
# In standalone mode, Tentackl uses local JWT auth + API keys.
# No InkPass or Mimic required.

# --- Security (REQUIRED) ---
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
TENTACKL_SECRET_KEY=change-me-to-a-random-secret-at-least-32-chars
SECRET_KEY=change-me-to-a-random-secret-at-least-32-chars

# Static API key for service-to-service or CLI access.
# Authenticate with: curl -H "X-API-Key: tk_dev_key" http://localhost:8000/api/tasks
TENTACKL_API_KEY=tk_dev_key

# --- LLM API Keys (at least one required for agent execution) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=

# --- Optional ---
# CORS_ORIGINS=http://localhost:3000
# LOG_LEVEL=INFO
# DEBUG=false
