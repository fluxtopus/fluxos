# Mimic Notification Service - Environment Configuration
# Copy this file to .env and update the values

# ===== Application Configuration =====
APP_ENV=development
LOG_LEVEL=INFO
PORT=8000

# ===== Database Configuration =====
# Local development (Docker Compose):
DATABASE_URL=postgresql://postgres:postgres_master_pass@postgres:5432/aios_mimic

# ===== Redis Configuration =====
# Local development (Docker Compose):
REDIS_URL=redis://redis:6379/6

# ===== Security Keys =====
# IMPORTANT: Generate new keys for production!
# Generate with: openssl rand -hex 32

# Secret key for general app security
SECRET_KEY=dev-secret-key-change-in-production

# Encryption key for sensitive data (must be 32 characters)
ENCRYPTION_KEY=dev-encryption-key-32-chars-long

# ===== JWT Configuration =====
# JWT algorithm
ALGORITHM=HS256

# Token expiration (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===== Service Integration =====
# URLs for other services (Docker Compose):
TENTACKL_URL=http://tentackl:8000
TENTACKL_API_KEY=

INKPASS_URL=http://inkpass:8000

# ===== Stripe Configuration (Optional) =====
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ===== Subscription Settings =====
FREE_TIER_NOTIFICATIONS_LIMIT=100
ANNUAL_SUBSCRIPTION_PRICE=49900

# ===== Production Recommendations =====
# 1. Generate strong random keys:
#    SECRET_KEY: openssl rand -hex 32
#    ENCRYPTION_KEY: openssl rand -base64 32 (ensure 32 chars)
#
# 2. Use strong database passwords
# 3. Set APP_ENV=production in production
# 4. Configure proper service URLs for your environment
# 5. Set up Stripe for billing if needed
# 6. Never commit .env file to version control
