# inkPass Environment Configuration
# Copy this file to .env and update the values

# ===== Database Configuration =====
DATABASE_URL=postgresql://inkpass:inkpass_dev_password@db:5432/inkpass

# ===== Redis Configuration =====
REDIS_URL=redis://redis:6379/0

# ===== Security Keys =====
# IMPORTANT: Generate new keys for production!
# Generate with: openssl rand -hex 32

# Encryption key for sensitive data (must be 32 characters)
ENCRYPTION_KEY=dev-encryption-key-32-chars-long

# Secret key for general app security
SECRET_KEY=dev-secret-key-change-in-production-please

# JWT secret key for token signing
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production

# ===== JWT Configuration =====
# Token expiration times (in minutes/days)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# JWT algorithm
ALGORITHM=HS256

# ===== Application Configuration =====
# Application environment (development, staging, production)
ENVIRONMENT=development

# Debug mode (true/false)
DEBUG=true

# API host and port
HOST=0.0.0.0
PORT=8000

# ===== CORS Configuration =====
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8002

# ===== Rate Limiting =====
# Redis database for rate limiting (0-15)
RATE_LIMIT_DB=1

# Requests per minute per IP
RATE_LIMIT_PER_MINUTE=60

# ===== Logging =====
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log format (json, console)
LOG_FORMAT=console

# ===== Email Configuration (for OTP) =====
# Currently mocked - configure for production
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@inkpass.example.com

# ===== Mimic Integration (Notifications) =====
# URL of Mimic notification service
MIMIC_URL=http://mimic:8001

# API key for InkPass to authenticate with Mimic
# Generate via Mimic dashboard or API: POST /api/v1/api-keys
MIMIC_API_KEY=

# ===== Production Recommendations =====
# 1. Generate strong random keys:
#    ENCRYPTION_KEY: openssl rand -base64 32
#    SECRET_KEY: openssl rand -hex 32
#    JWT_SECRET_KEY: openssl rand -hex 32
#
# 2. Use strong database passwords
# 3. Enable SSL/TLS for database connections in production
# 4. Set DEBUG=false in production
# 5. Configure proper CORS_ORIGINS
# 6. Set up proper SMTP for email delivery
# 7. Use environment-specific values for each deployment
# 8. Never commit .env file to version control
